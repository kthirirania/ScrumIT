import 'package:flutter/material.dart';

class BacklogsTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      color: Colors.blueGrey,
      child: Text(
        'Backlogs'
      ),
    );
  }
}
